/* ============================================================
   COLSON FELLOWS SINGAPORE — EDIT THIS FILE EACH YEAR
   ============================================================
   This is the ONLY file you should need to touch between
   cohorts. Change the text between the quote marks on the
   right of each line below — leave the quote marks, commas,
   and everything else exactly as it is.

   Every page on the site reads its dates and figures from
   here. Save this file, re-upload it (or push it) to your
   repo, and the whole site updates itself — no need to open
   any of the .html files.
   ============================================================ */

const SITE_CONFIG = {

  // Shown in the header bar and various headings, e.g. "2026–2027"
  cohortLabel: "2026–2027",

  // Plain-English deadline, shown in copy — e.g. "31 July 2026"
  applicationDeadline: "31 July 2026",

  // The SAME date as above, in YYYY-MM-DD format. This is what the
  // site actually checks to decide which messaging to show — keep
  // it in sync with the line above.
  applicationDeadlineISO: "2026-07-31",

  // When the cohort begins, in plain English — e.g. "August 2026"
  cohortStart: "August 2026",

  // When the cohort finishes / commissioning month — e.g. "May 2027"
  commissioning: "May 2027",

  // Tuition
  tuitionUSD: "USD 900",
  tuitionSGD: "SGD 1,160",
  booksUSD: "USD 200",

  // Roughly tuitionUSD ÷ number of fortnightly sessions in the
  // programme (about 20). Recalculate this by hand if tuition changes.
  perSessionCost: "about USD 45"

};
